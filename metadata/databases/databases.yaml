- name: appcore
  kind: postgres
  configuration:
    connection_info:
      database_url:
        from_env: APPCORE_DATABASE_URL
      isolation_level: read-committed
      use_prepared_statements: false
  tables: "!include appcore/tables/tables.yaml"
