#!/bin/bash

# display help
function help() {
  case $1 in
    env)
      echo "display environment"
      ;;
    *)
      echo "  $RUNNAME env"
      ;;
  esac
}

# run something
case $1 in
  help) help ${@:2};;
  env) cleanroom env;;
esac
