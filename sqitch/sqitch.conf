[core]
	engine = pg
	plan_file = sqitch.plan
	top_dir = .
[engine "pg"]
	target = db:pg://postgres:postgres@localhost:5432/local
	registry = sqitch
	client = psql
[deploy]
	verify = true
[rebase]
	verify = true
