table:
  name: codes
  schema: offer
object_relationships:
  - name: activation
    using:
      foreign_key_constraint_on:
        column: code
        table:
          name: activations
          schema: offer
