table:
  name: events
  schema: trigger
array_relationships:
  - name: hooks
    using:
      foreign_key_constraint_on:
        column: event_id
        table:
          name: hooks
          schema: trigger
  - name: logs
    using:
      foreign_key_constraint_on:
        column: event_id
        table:
          name: logs
          schema: trigger
