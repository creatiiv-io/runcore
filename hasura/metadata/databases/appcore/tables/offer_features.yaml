table:
  name: features
  schema: offer
object_relationships:
  - name: default
    using:
      foreign_key_constraint_on: feature_id
  - name: plan
    using:
      foreign_key_constraint_on: plan_id
