table:
  name: preferences
  schema: setup
array_relationships:
  - name: inherited_by
    using:
      foreign_key_constraint_on:
        column: preference_id
        table:
          name: preferences
          schema: client
